const tmp = `<main class="card">
    <div class="register">
        <h1 class="register__title">Registration</h1>
        {{{ registerForm}}}
        <div class="register__footer-footer">
            {{{ authLink }}}
        </div>
    </div>
</main>`;

export default tmp;
