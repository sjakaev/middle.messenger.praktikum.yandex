const tmp = `
    <main class="card">
        <div class="login">
            <h1 class="login__title">Authorization</h1>
            {{{ loginForm }}}

            <div class="login__footer">
                {{{ link }}}
            </div>
        </div>
    </main>
`;

export default tmp;
